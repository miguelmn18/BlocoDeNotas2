/*
import static org.junit.jupiter.api.Assertions.*;

class BlocoDeNotasTest {

    @org.junit.jupiter.api.Test
    void adicionaBlocoDeNotas(String texto) {
       try {
           adicionaBlocoDeNotas(" ");
           fail("Não era pra chegar até aqui, tinha que ser lançada uma exceção");
       } catch (Exception e) {
           assertEquals("A nota não pode ser vazia ou nula." + e.getMessage());
       }

    }

    @org.junit.jupiter.api.Test
    void editaAnotacao(String texto, int i) {
        try {
            editaAnotacao("Quero comer doce", -1 );
            fail("Não era pra chegar até aqui, tinha que ser lançada uma exceção.");
        } catch (Exception e){
            assertEquals("É preciso informar o indice da anotação que deseja mudar." + e.getMessage());
        }
    }
}*/
